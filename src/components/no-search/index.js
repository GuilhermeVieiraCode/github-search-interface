import React from 'react';
import * as Styled from './styled'

export const NoSearch = () => {
    return(
        <Styled.Wrapper>
            <h1>No username searched</h1>
        </Styled.Wrapper>
    );
}